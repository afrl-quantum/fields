# this Jamroot is mostly for helping build unit-tests while developing in the
# source

project /fields/unit-tests
    : requirements
        <library>/boost//unit_test_framework/<link>static
        <library>/fields//headers
    ;

use-project /fields : ../ ;

build-project fields ;
